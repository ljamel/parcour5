<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<markers>
    {% for article in articles %}
        <marker id="{{ article.id }}" name="{{ article.title }}" address="1/28 Macleay Street, Elizabeth Bay, NSW" lat={{ article.position|replace({",": " lng=", '%that%':80}) }} type="restaurant"/>
    {% endfor %}
</markers>






