{% extends "/layout.html.twig" %}

{% block content %}

    <div class="title-show">
        <div>
            <h2>{{ loisir.title }}</h2>
            <img class="liste-main-unique" src="{{ loisir.image }}">
            <p class="texte-show">{{ loisir.content }}</p>
        </div>
        <div class="info-unique">
            NOTE : {{ loisir.note }}/10 <br />
            <a href="#map"> Voire sur le plans </a>


            <h3>Commentaires</h3>
            {% for comment in comments %}
                <strong>{{ comment.author.username }}</strong> : {{ comment.content }} <a href="/comment/{{ comment.id }}/signale">: Signaler le commentaire</a><br>

            {% else %}
                Aucun commentaire trouvez.
            {% endfor %}

            <h3>Ajouter un commentaire</h3>
            {% if commentForm %}
                {{ form_start(commentForm) }}
                <div class="form-group">
                    {{ form_errors(commentForm.content) }}
                    {{ form_widget(commentForm.content, { 'attr':  {
                        'rows': '4',
                        'class': 'form-control',
                        'placeholder': 'Enter your comment'
                    }}) }}
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Publier" />
                </div>
                {{ form_end(commentForm) }}
                {% for flashMessage in app.session.flashbag.get('success') %}
                    <div class="alert alert-success">
                        {{ flashMessage }}
                    </div>
                {% endfor %}
            {% else %}
                <a href="{{ path('login') }} ">Log in</a> to add comments.
            {% endif %}
        </div>
    </div>


    <div id="map"></div>


<!-- script qui appel la map -->
<script src="https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/markerclusterer.js"></script>
<script async defer src="https://maps.googleapis.com/maps/api/js?v=3&key=AIzaSyCLCyZdCatCyO348_1vXQr4yEQkDheyQ3A&callback=initMap&posi=lat:{{ loisir.position|replace({",": ", lng: ", '%that%': 48}) }}"></script>
<script src="http://localhost/parcour-5/web/js/ajax.js"></script>
<script src="http://localhost/parcour-5/web/js/map-unique.js"></script>
<script src="http://localhost/parcour-5/web/js/menu.js"></script>
{% endblock %}
